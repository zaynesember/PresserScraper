.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store
fails.csv
*.tar.gz
..Rcheck/
docs/

# Session scratch — not part of the package
SESSION_PROGRESS.md

# Blog: large external congressional-comms datasets (tweets / CrowdTangle-Facebook /
# newsletters) — ~8.7 GB, derived/third-party, not for git. Keep blog/ itself trackable
# (posts, notebooks, reference PDFs) but never the bulk data.
blog/data/

# NARA crawler outputs (units table, walk/extract results) — collected data, not code.
nara/data/
