% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape.R
\name{scrape_senate}
\alias{scrape_senate}
\title{Scrape press releases for the whole U.S. Senate}
\usage{
scrape_senate(
  from,
  to = Sys.Date(),
  max_members = NULL,
  fetch_bodies = TRUE,
  page_limit = 100,
  render = c("auto", "never", "always"),
  log_fails = FALSE,
  fails_path = "fails.csv",
  quiet = FALSE
)
}
\arguments{
\item{from, to}{Inclusive date range. \code{to} defaults to today.}

\item{max_members}{Optionally cap the number of senators (e.g. for a quick
sample); \code{NULL} scrapes everyone.}

\item{fetch_bodies}{Whether to fetch each release's body text.}

\item{page_limit}{Per-member maximum listing pages to walk.}

\item{render}{Headless-browser policy passed to \code{\link[=scrape_member]{scrape_member()}}
(\code{"auto"}, \code{"never"}, or \code{"always"}). For large batch runs \code{"never"}
avoids spawning a browser on the unsupported tail; the default \code{"auto"}
only renders sites that yield nothing statically (and only if
\pkg{chromote} is installed).}

\item{log_fails}{If \code{TRUE}, write the failures table to \code{fails_path}.}

\item{fails_path}{Path for the failures CSV when \code{log_fails = TRUE}.}

\item{quiet}{Suppress per-member progress messages.}
}
\value{
See \code{\link[=scrape_pressers]{scrape_pressers()}}.
}
\description{
Convenience wrapper that pulls the current senator directory with
\code{\link[=list_senators]{list_senators()}} and scrapes it with \code{\link[=scrape_pressers]{scrape_pressers()}}.
}
\seealso{
\code{\link[=scrape_house]{scrape_house()}} for the House.
}
