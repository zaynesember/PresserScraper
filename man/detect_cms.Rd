% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_cms.R
\name{detect_cms}
\alias{detect_cms}
\title{Detect the content-management system behind a member site}
\usage{
detect_cms(url, doc = NULL)
}
\arguments{
\item{url}{Member homepage URL. Ignored if \code{doc} is supplied.}

\item{doc}{Optional pre-fetched \code{xml_document} (avoids a second request).}
}
\value{
One of \code{"drupal"}, \code{"aspx"}, \code{"wordpress"}, \code{"guid"}, \code{"generic"},
or \code{"unknown"} (when the page could not be fetched).
}
\description{
Inspects a homepage's \verb{<meta name="generator">} tag and a set of markup
fingerprints to classify the site. About 86\% of House sites fall into three
vendor families (Drupal, ASP.NET, WordPress); everything else is handled by
the \code{"generic"} extractor.
}
\examples{
\dontrun{
detect_cms("https://barrymoore.house.gov")  # "drupal"
detect_cms("https://carbajal.house.gov")    # "aspx"
}
}
