% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/senators.R
\name{list_senators}
\alias{list_senators}
\title{List current U.S. Senators and their websites}
\usage{
list_senators(
  url = "https://www.senate.gov/general/contact_information/senators_cfm.xml",
  committees = TRUE
)
}
\arguments{
\item{url}{The Senate directory XML (override only for testing).}

\item{committees}{Whether to enrich each senator with their committee
assignments (a \code{";"}-delimited string in the \code{committee} column). Requires
one extra fetch of the public \@unitedstates congressional dataset
(\url{https://github.com/unitedstates/congress-legislators}); if it is
unavailable, \code{committee} is left \code{NA} (no error).}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{name}, \code{state}, \code{district},
\code{party}, \code{committee}, \code{url}, and \code{chamber} (\code{"senate"}).
}
\description{
Parses the Senate's contact-information XML feed (one entry per senator with
name, party, state, and website). Senators are statewide, so \code{district} is
\code{NA}; committee assignments are not in this feed, so \code{committee} is \code{NA}.
The returned columns mirror \code{\link[=list_members]{list_members()}} so House and Senate results can
be combined or scraped with the same machinery.
}
\examples{
\dontrun{
senators <- list_senators()
}
}
\seealso{
\code{\link[=list_members]{list_members()}} for the House, \code{\link[=scrape_senate]{scrape_senate()}} to scrape them.
}
