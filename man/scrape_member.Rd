% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_member.R
\name{scrape_member}
\alias{scrape_member}
\title{Scrape one member's press releases within a date range}
\usage{
scrape_member(
  url,
  from,
  to = Sys.Date(),
  cms = NULL,
  page_limit = 100,
  fetch_bodies = TRUE,
  render = c("auto", "never", "always"),
  quiet = FALSE
)
}
\arguments{
\item{url}{Member homepage (e.g. \code{"barrymoore.house.gov"}).}

\item{from, to}{Date range (inclusive). Character \code{"YYYY-MM-DD"} or \code{Date}.
\code{to} defaults to today.}

\item{cms}{Optional CMS override (\code{"drupal"}, \code{"aspx"}, \code{"wordpress"},
\code{"generic"}); detected automatically when \code{NULL}.}

\item{page_limit}{Maximum listing pages to walk (safety bound).}

\item{fetch_bodies}{If \code{FALSE}, skip fetching each release's body text
(faster; \code{body}/\code{tags} come back \code{NA}).}

\item{render}{Headless-browser policy for JS-rendered sites. \code{"auto"} (the
default) does a static pass and, only if it finds \emph{no} parseable releases
at all, retries once through a headless browser (when the suggested
\pkg{chromote} package is installed). \code{"never"} disables the fallback;
\code{"always"} renders from the start. The auto fallback does not fire merely
because a site has no releases in the date window.}

\item{quiet}{Suppress progress messages.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{date}, \code{title}, \code{body},
\code{tags}, \code{url}, \code{cms}. Zero rows if nothing falls in range.
}
\description{
Detects the member site's CMS, finds its press-release listing, walks the
listing pages newest-first until it passes the start of the window, and
fetches the body of each release in range.
}
