% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive.R
\name{archive_releases}
\alias{archive_releases}
\title{Append scraped releases to the on-disk archive}
\usage{
archive_releases(releases, dir = archive_dir(), quiet = FALSE)
}
\arguments{
\item{releases}{A releases tibble from \code{\link[=scrape_member]{scrape_member()}}, \code{\link[=scrape_pressers]{scrape_pressers()}},
or \code{\link[=scrape_house]{scrape_house()}} (must have at least \code{date} and \code{url} columns).}

\item{dir}{Archive directory. Defaults to \code{getOption("pressR.archive_dir")}
or, failing that, \code{tools::R_user_dir("pressR", "data")}.}

\item{quiet}{Suppress the summary message.}
}
\value{
Invisibly, a list with \code{dir}, \code{added}, and \code{updated} counts.
}
\description{
Writes releases into a local, year-partitioned store, de-duplicating by
\code{url} so re-running a scrape refreshes existing rows rather than duplicating
them. Builds up historical coverage across runs.
}
\seealso{
\code{\link[=read_archive]{read_archive()}}, \code{\link[=publish_archive]{publish_archive()}}.
}
