% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape.R
\name{scrape_pressers}
\alias{scrape_pressers}
\title{Scrape press releases for many members}
\usage{
scrape_pressers(
  members,
  from,
  to = Sys.Date(),
  fetch_bodies = TRUE,
  page_limit = 100,
  log_fails = FALSE,
  fails_path = "fails.csv",
  quiet = FALSE
)
}
\arguments{
\item{members}{Either a character vector of member URLs (optionally named,
in which case names become the \code{name} column) or a data frame with a \code{url}
column plus any metadata columns to carry through (e.g. from
\code{\link[=list_members]{list_members()}}).}

\item{from, to}{Inclusive date range. \code{to} defaults to today.}

\item{fetch_bodies}{Whether to fetch each release's body text.}

\item{page_limit}{Per-member maximum listing pages to walk.}

\item{log_fails}{If \code{TRUE}, write the failures table to \code{fails_path}.}

\item{fails_path}{Path for the failures CSV when \code{log_fails = TRUE}.}

\item{quiet}{Suppress per-member progress messages.}
}
\value{
A \link[tibble:tibble]{tibble} of releases (member metadata columns,
then \code{date}, \code{title}, \code{body}, \code{tags}, \code{url}, \code{cms}). A \code{failures} tibble
(\code{url}, \code{stage}, \code{message}) is attached as an attribute; retrieve it with
\code{attr(result, "failures")}.
}
\description{
Runs \code{\link[=scrape_member]{scrape_member()}} across a set of member sites, isolating failures so
one broken site doesn't stop the run. Member metadata (name, state, etc.) is
carried through when \code{members} is a data frame such as that from
\code{\link[=list_members]{list_members()}}.
}
\seealso{
\code{\link[=scrape_house]{scrape_house()}} to scrape the whole chamber.
}
